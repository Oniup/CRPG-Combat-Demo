FILE(GLOB_RECURSE SOURCE_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR} "*.cc")
FILE(GLOB_RECURSE HEADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR} "*.h")

TARGET_SOURCES(${PROJECT_NAME}
PRIVATE
    ${SOURCE_FILES}
    ${HEADER_FILES}
)
